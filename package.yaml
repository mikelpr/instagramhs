name:    instagram-scraper
version: "0.0.1"

dependencies:
- base
- aeson
- bytestring
- containers
- directory
- http-client-tls
- http-conduit
- text

executables:
  instagramhs:
    main: Main.hs
    source-dirs: app
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N